
function setup() {
	createCanvas(100,100);//(1280, 720) //(displayWidth, displayHeight);

}

function draw() {
	background(0);

	// // particles show up
	// for(var i = 0; i < systems.length; i++) {
	// 	systems[i].update();
	// 	systems[i].render();
	// }

	// // reset particle position
	// if(cnt == 35) {
	// 	for(var i = 0; i < systems.length; i++) {
	// 		systems[i].setpos();
	// 	}
	// }

	// // add axis
	// if(cnt > 35) {
	// 	for(var i = 0; i < systems.length; i++) {
	// 		systems[i].update_axis();
	// 		systems[i].render_axis();
	// 	}
	// 	opac = opac + 2;
	// 	fill(160, opac);
	// 	textSize(21);
	// 	text("NUMBER OF SUICIDE IN SOUTH KOREA", 72, 75);
	// 	fill(255, 153, 0, opac);
	// 	text("2009", 72 + 425, 75);
	// }

	// // add arrow
	// if(cnt > 50) {
	// 	update_arrow();
	// 	render_arrow();

	// 	check_selected();
	// }

	// // control variable
	// cnt++;
	// if(opac > 225) {
	// 	opac = 225;
	// }
}
